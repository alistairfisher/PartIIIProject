package Geocoding.ImitationLearning;

import Geocoding.Example;

import java.util.ArrayList;

/**
 * Created by alistair on 30/03/2017.
 */
public class DAGGER {
/*
    static Policy learn(ArrayList<Example> examples, LossFunction l, CSCLearner csc, double learningRate, int N) {
        ArrayList<CSCExample> cscExamples = new ArrayList<>();
        Policy expertPolicy = new Policy(new Prediction(examples));
        for (int i = 0; i < N; i++) {
            double p = Math.pow((1-learningRate),i);
            for (Example e: examples) {

            }
        }
        return expertPolicy;
    }*/

}
